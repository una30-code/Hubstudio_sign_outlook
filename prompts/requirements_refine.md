# 需求细化 → 写入 requirements.md

## 提示词正文（复制以下）

```
请根据我的描述，整理并写入本仓库的 `requirements.md`（只改该文档，不写 `src/`）。风格与现有章节保持一致：阶段目标、功能需求表（ID/描述/验收标准）、输入输出、非功能（超时、失败不崩溃、关键日志、失败截图到根目录 `screenshots/`、配置走 `.env` 不落密钥）、范围外、成功定义与失败定义、变更记录。

当前仓库已有 Phase 1 范式：Hubstudio + Playwright CDP，打开 Outlook 注册页并验证加载；成功需同时满足「URL 符合约定」与「出现注册相关元素」。若我的描述与上述冲突或要开启 Phase 2，在文档里明确新版本目标并更新变更记录。

若我的表述有歧义，先列出待确认问题，再给一版可落地的草案。

我的新需求或变更说明：
【粘贴：场景、必须/禁止做的事、验收口径、是否批量等】
```
